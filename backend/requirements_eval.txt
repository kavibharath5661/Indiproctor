# ─── requirements_eval.txt ───────────────────────────────────────────────────
# Install all dependencies for the evaluation script.
# Run:  pip install -r requirements_eval.txt
# ─────────────────────────────────────────────────────────────────────────────

# Core scientific stack
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7

# Computer Vision & AI
opencv-python>=4.8
mediapipe>=0.10          # Face mesh / face detection

# Web framework (for app.py — not needed for evaluation alone)
flask>=3.0
flask-cors>=4.0
flask-socketio>=5.3
python-socketio>=5.10

# Encryption
cryptography>=41.0

# Object detection (optional — only for phone_detector.py)
# ultralytics>=8.0       # Uncomment after: pip install ultralytics

# Audio (optional — only if you run the live Flask server)
# pyaudio>=0.2.13        # Uncomment and install portaudio first
